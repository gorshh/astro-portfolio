---
import Link from "./Link.astro";
---

<Link class="logo" href="#home">J</Link>

<style>
  .logo {
    font-family: "WienModern";
    height: 40px;
    width: 40px;
    background-color: var(--white-color);
    color: var(--bg-color);
    display: flex;
    align-items: center;
    justify-content: center;
    padding-top: 2px;
    border-radius: 8px;
    font-size: 20px;
    line-height: 0;
    user-select: none;

    &::after {
      display: none;
    }

    &:hover {
      color: rgba(var(--white-rgb), 0.8);
      background-color: var(--bg-color);
    }
  }
</style>
